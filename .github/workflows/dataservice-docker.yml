name: DataService Docker Build and Push

env:
    REGISTRY: ghcr.io
    IMAGE_NAME: ${{ github.repository }}

on:
    push:
        branches: [ "main" ]
        paths:
          - 'FantasyFootballManager.DataService/**'

jobs:
    build-and-push-image:
        runs-on: ubuntu-latest
        defaults:
            run:
                working-directory: ./FantasyFootballManager.DataService
        permissions:
            contents: read
            packages: write
        steps:
        - name: Setup QEMU
          uses: docker/setup-qemu-action@v3.0.0

        - name: Set up Docker Buildx
          uses: docker/setup-buildx-action@v3.3.0
          with:
            platforms: linux/amd64,linux/arm64

        - name: Log into registry
          uses: docker/login-action@v2.2.0
          with:
            registry: ${{ env.REGISTRY }}
            username: ${{ github.actor }}
            password: ${{ secrets.GITHUB_TOKEN }}

        - name: Extract metadata (tags, labels) for Docker
          id: meta
          uses: docker/metadata-action@9ec57ed1fcdbf14dcef7dfbe97b2010124a938b7
          with:
              images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}

        - name: Build and push Docker image
          uses: docker/build-push-action@v5.3.0
          with:
            context: "{{defaultContext}}:FantasyFootballManager.DataService"
            platforms: linux/amd64,linux/arm64
            push: true
            tags: |
                ghcr.io/ckriutz/fantasyfootballmanager:dataservice-latest
                ghcr.io/ckriutz/fantasyfootballmanager:dataservice.${{ github.sha }}
            labels: ${{ steps.meta.outputs.tags }}